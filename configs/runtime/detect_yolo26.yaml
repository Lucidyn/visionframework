pipeline: detection

models:
  detector: configs/models/yolo26n.yaml

# 转换后的权重路径
# python tools/convert_ultralytics.py --model yolo26n.pt --out weights/yolo26n_converted.pth
weights: weights/yolo26n_converted.pth

device: auto
fp16: false
