# Minimal detector configuration example for the examples folder
detector:
  model_type: "yolo"
  model_path: "yolov8n.pt"
  conf_threshold: 0.25
  iou_threshold: 0.45
  device: "cpu"
  # Optional: restrict detections to classes by name or id
  categories: null

pipeline:
  enable_tracking: false
